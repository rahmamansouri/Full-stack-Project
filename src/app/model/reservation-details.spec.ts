import { ReservationDetails } from './reservation-details';

describe('ReservationDetails', () => {
  it('should create an instance', () => {
    expect(new ReservationDetails()).toBeTruthy();
  });
});
